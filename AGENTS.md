# Human Experience Lab workspace rule

- All files related to Human Experience Lab must be created, edited, and organized inside `/Users/zhang/Documents/Human_Experience_Lab`.
- Do not create duplicate `Human_Experience_Lab` folders or project deliverables elsewhere.
- Put new content into the repository's existing relevant directory whenever one exists.
- Use `Research_Notes/` for training and research notes, `Templates/` for templates, `knowledge/` for reference knowledge, `custom-gpt/` for Custom GPT configuration, and `prompts/` for prompt libraries.
- Preserve existing files and merge carefully; do not overwrite unrelated user content.
