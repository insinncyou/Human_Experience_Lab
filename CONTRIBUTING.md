# コントリビューションガイド

Human Experience Lab への貢献に関心を持っていただき、ありがとうございます。このリポジトリでは、JTBD を中心とした人間の経験・行動・意思決定に関する学びを、再利用しやすい Markdown として蓄積します。

## 貢献できる内容

- JTBD、UX 心理学、行動設計、サービス設計、プロダクト戦略に関する研究ノート
- ケース分析、インタビュー練習、失敗例、パターン、フレームワーク
- 既存資料の誤りの修正、構成の改善、重複内容の統合
- 再利用可能なテンプレート、プロンプト、Custom GPT 向け Knowledge

## 保存先

内容に合う既存ディレクトリを選んでください。

| 内容 | 保存先 |
| --- | --- |
| 日々の学習・研究ノート | `Research_Notes/` |
| 再利用可能な書式 | `Templates/` |
| 参照用 Knowledge | `knowledge/` |
| Custom GPT の設定 | `custom-gpt/` |
| プロンプト集 | `prompts/` |
| JTBD の専門資料 | `JTBD/` 配下の該当カテゴリ |

既存の分類で扱える場合は、新しいトップレベルディレクトリを作らないでください。

## 作業の始め方

1. 最新の `main` を取得します。
2. 1 つの目的に絞った短命なブランチを作ります。
3. 既存資料を確認し、重複を避けて追加または統合します。
4. 変更内容を確認してから、対象ファイルだけをコミットします。
5. ブランチを push し、Pull Request を作成します。

詳しいコマンドと命名規則は [Git ワークフロー](docs/git-workflow.md) を参照してください。

## コンテンツの基準

- 原則として 1 ファイル 1 テーマにします。
- 結論、根拠、具体例、次に試すことが分かる構成を推奨します。
- 事実、引用、自分の解釈を区別します。
- 外部資料を参照した場合は、出典名と URL を明記します。
- 長い引用や権利上問題のある転載は避け、自分の言葉で要約します。
- 個人情報、顧客情報、秘密情報、認証情報を含めないでください。

## Markdown とファイル名

- 見出しは `#` から順番に使用し、段階を飛ばさないでください。
- リンクには内容が分かるラベルを付けます。
- コード、プロンプト、発話例は fenced code block を使います。
- 既存ディレクトリの命名規則を優先します。
- 日付を含める場合は、並び替えやすい `YYYY-MM-DD-short-topic.md` を推奨します。

## Pull Request

- 変更の目的と背景を簡潔に説明してください。
- 主な変更点と影響するディレクトリを記載してください。
- 大きな変更は、レビューしやすい単位に分割してください。
- レビューコメントに対応した場合は、必要に応じて変更理由も残してください。
- `main` へ直接 push せず、Pull Request を通して統合してください。

Pull Request を出す前に、リンク、見出し、差分、機密情報の有無を確認してください。
